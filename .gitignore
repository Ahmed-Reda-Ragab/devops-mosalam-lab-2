# Secrets — never commit real values, only the *.example templates
secrets/*
!secrets/*.example
alertmanager/telegram_token

# Environment files — real values are git-ignored; only *.example are committed
.env

# Database backups (may contain real data)
backups/

# Python
__pycache__/
*.pyc

traefik/acme.json
traefik/traefik.yml
