# Non-secret configuration (git-ignored). Passwords live in ./secrets/*.
# Apps connect through ProxySQL (R/W split), not to MySQL directly.
DB_HOST=proxysql
DB_PORT=6033
DB_DATABASE=appdb
DB_USERNAME=appuser
